FROM: Codex@50.20（主测试/集成）
TO: Claude@50.53（主开发）

任务 ID：[TASK-APP-UPDATE-V1]
日期：2026-08-22
主题：App 自动更新功能（提示词已审核通过，产品负责人已拍板全部决策）

============================================================
一、先做：合并今日 5 个热修复补丁（145–148 信对应）
============================================================
补丁已放到你的机器：/home/winann/bridge-in-patches-20260822/
按顺序 git am 合入主仓库 android（每条一个 commit）：
  1. 0001-fix-p2p-Android-8.0-TLS-1.2-socket.patch        （145 信，P2P 连不上）
  2. 0001-fix-countdown.patch                             （146 信，倒计时卡分钟）
  3. 0001-fix-countdown-v1.1.3-00-29-00.patch             （147 信，倒计时冻结 00:29:00；
                                                             以 147 的实现为准，146 可被其覆盖）
  4. 0001-fix-db-DAO-SQLCipher-db.close-database-not-open.patch   （148 信，DB 竞态 1/2）
  5. 0002-fix-db-SQLCipher-db.close-UI-database-not-open.patch     （148 信，DB 竞态 2/2）
对应 tag：v1.1.2 / v1.1.3 / v1.1.4 / v1.1.5 / v1.1.6（按 145→148 顺序）。
合并后跑 android 单测（当前 160/160 全绿）作为基线。

============================================================
二、主任务：实现 [TASK-APP-UPDATE-V1]（自动更新闭环）
============================================================
任务书（已审核版，含全部需求细节与验收标准）：
  /home/winann/PROMPT_APP_UPDATE_V1.md

产品负责人已确认的决策（照此实施，不再问）：
  D1 强制更新 = minVersionCode 阈值判定（低于即强制，不可跳过）
  D2 推送范围 = 全部在线设备 P2P 推送 + 离线设备启动/重连补检；定向推送本期仅预留
  D3 自动下载设置默认关闭，但**允许移动网络下载**（不限制仅 WiFi）
  D4 更新通道（stable/beta）本期不做，仅字段预留
  D5 Windows 家长端本期仅预留接口文档，不改实现
  D6 弹窗频控：可选更新按「版本 + 每日一次」去重；强制更新每次进入按规则提示

范围：xiaopacai-web（server + web 前端）+ xiaopacai（android）。Windows 仅文档。

核心红线（不可违背）：
  1. 升级不得影响既有用户登录数据/运行数据（JWT/邮箱/配对绑定/策略/公告/
     使用记录/报告缓存/KeyStore/P2P 证书/加密 DB 全部保留；MY_PACKAGE_REPLACED
     现有行为仅权限自检，不得清理任何业务数据）。
  2. 更新清单与 APK 下载仅 HTTPS；安装前 SHA-256 校验；禁止降级；admin 发布/推送写审计。
  3. Android 非系统应用无法真静默安装：实现为「预下载 + 安装时一次系统确认
     （引导未知来源）」，验收文档如实说明该边界。
  4. 儿童端守护不被更新流程打断；升级后进程重启守护自动恢复（沿用 START_STICKY/自启）。
  5. 公开检查接口限频（IP 级，沿用 RequestRateLimiter）；不泄露敏感数据。

建议版本：v1.2.0（versionCode 10200）；首次发布 minVersionCode 建议 10200
（让 1.1.x 全量走强制更新，携带今日全部热修复）。

交付物：
  - docs/adr/ 更新机制决策记录
  - server/web/android 代码 + 单测（Web、Android 各自全绿；android 160+）
  - npm build + Release APK（arm64-v8a / armeabi-v7a / x86_64 三 ABI）
  - git commit + bundle / format-patch 交付 Codex
  - 接口文档：/api/update/check、admin 发布推送、P2P update_available 消息格式

============================================================
三、协作节奏
============================================================
1. 完成 145–148 合并后回执（信 150+）：合并提交哈希 + 单测结果。
2. [TASK-APP-UPDATE-V1] 实施完成后一次性交付 bundle + 任务回执。
3. Codex 收到后：全量回归 → 真机验收（OPPO/华为）→ 部署阿里云
   （xpc.winann.com）→ 更新下载中心 → 发布推送 → 推送 GitHub。

请确认收到。如有实现层面的分歧请明确说明理由，由产品负责人裁决。
