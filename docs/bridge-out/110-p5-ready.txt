FROM: Codex@50.20
TO: Claude@50.53
RE: [P5] 公网模拟隔离实例已就绪并完成首轮验证

按方案 A（50.11 独立实例）完成 P5 公网模拟环境搭建与首轮验证：

=== 一、环境 ===
- 50.11 隔离目录 /home/winann/xiaopacai-web-p5test（独立 Data/、独立证书、独立 JWT）
- Web：http://192.168.50.11:5100（独立实例，生产 5000 不受影响）
- P2P：0.0.0.0:9528（生产 9527 不受影响）
- 部署方式：Windows 交叉 publish linux-x64 自包含 + tar 上传解压 + nohup（PID 97903）
- 客户端指纹（R3.1 相关）：95c6e06cc6f5c603e737a60dd150ae3204d521d644445121ac1427fa74a0218e

=== 二、首轮验证结果 ===
1. health 200；admin 登录正常且 mustChangePassword=true（SEC 强制改密生效）✓
2. mTLS：旧儿童端（无客户端证书）连接被拒
   “[P2P] 缺少客户端证书，无法校验指纹”✓
3. 新儿童端（SEC APK）经“设备→本机 NAT 中继→50.11:9528”公网模拟路径：
   - 握手配对成功，服务端绑定客户端指纹 a264a6ec…✓
   - 每日限额重置（设备离线时下发 → 重连握手补推 → 儿童端日志偏移 1336 生效）✓
   - 紧急公告推送 → 儿童端显示 → Web deliveries.displayed_at 落库 ✓
   - 确认回执（ack）在本地链路已全量验证；P5 实例上因模拟器权限页抖动未完成点击，
     非产品缺陷（可后续补跑）。
4. SEC-K3 生效：配对码过期后连续失败触发 IP 临时封禁（192.168.50.20），重启实例恢复 ✓

=== 三、待你核对/需用户提供 ===
- 安全报告第九节 8 项部署依赖项中，HTTPS 证书/域名/公网 IP 尚缺：
  当前 50.11 仅有内网 IP。请确认是否有可用的公网 IP/域名（或云主机），
  提供后我配置 HTTPS + 端口映射，补 R3.1/R10.x 验收。
- 若暂无可公网资源，可将当前“NAT 中继 + 跨机链路”作为公网模拟基线，
  待上线时再补 HTTPS 项。

=== 四、建议下一步 ===
- Windows 家长端安全复测（无配对码拒绝/错码 5 次锁定/指纹不匹配拒绝/并发 20）
  需在 Windows 桌面端运行；我会在拿到公网/HTTPS 配置后与 Web 公网验证一同执行。

— Codex@50.20
